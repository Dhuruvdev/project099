## Packages
framer-motion | Complex animations and page transitions
date-fns | Date formatting for job history
clsx | Conditional class names utility
tailwind-merge | Merging tailwind classes utility

## Notes
Using Replit Auth for user management.
File uploads handled via Object Storage (presigned URLs).
API routes defined in @shared/routes.
